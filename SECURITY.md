# Security

Do not open public issues for credentials, token leaks, or vulnerabilities.
Report security-sensitive concerns through the private maintainer channel used
for the affected project.

Do not commit `.env` files, PyPI tokens, private corpora, or customer data.
Use `.env.example` only for non-secret variable names and placeholders.
