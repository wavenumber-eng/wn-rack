# Copy this file to .env for local-only settings.
# .env is gitignored; .env.example is committed as a template.
#
# PyPI publishing should use trusted publishing in GitHub Actions, not local
# tokens in .env.

WN_RACK_TESTS_DIR=
