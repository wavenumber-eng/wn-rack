# Copy this file to .env and fill in the token value.
# .env is gitignored; .env.example is committed as a template.
#
# For PyPI API tokens, username is the literal string __token__
# and the password is the full token string (starts with "pypi-").

TWINE_USERNAME=__token__
TWINE_PASSWORD=pypi-REPLACE_WITH_YOUR_TOKEN
